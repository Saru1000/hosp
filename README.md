# Hospitality & Aviation Analytics Dashboard

Stable build that **avoids heavy-build libraries** (Prophet, OR‑Tools) so Streamlit Cloud installs cleanly.

* Python 3.11 (`runtime.txt`)
* Forecasting via **ARIMA** and **Holt‑Winters Exponential Smoothing** (both in `statsmodels`)